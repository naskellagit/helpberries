{
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "googleapis": "^150.0.1",
    "nodemon": "^3.1.10"
  },
  "scripts": {
    "front-dev": "npm run dev --prefix front",
    "front-install-packages": "npm i --prefix front",
    "front_build": "npm run build --prefix front",
    "back-dev": "nodemon server.js",
    "start": "node server.js"
  }
}
